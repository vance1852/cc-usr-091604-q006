# 体育场地服务

这是体育中心场地运营服务的 Python 起点。`app/venues.py` 目前只描述场地和开放状态，维护封闭、分区占用与预约流程需要在此基础上实现。

## 运行

```bash
python -m unittest discover -s tests -v
```

## 目录

- `app/venues.py`：场地基础对象
- `tests/`：基础行为测试

